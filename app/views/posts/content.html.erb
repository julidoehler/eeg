<% content_for :html_title, @post.title.upcase %>

<% if signed_in? %>
  <div class="admin_panel">
    <%= link_to 'Edit', edit_element_path(@element), :class => 'admin_control' %>
    <%= link_to 'Destroy', @element, :confirm => 'Are you sure?', :method => :delete, :class => 'admin_control' %>
  </div>
<% end %>

<div class="c">
  <div class="c_element_show color2">
    <h1><%=h @element.title.upcase %></h1>
    <%= render_element_show(@element) %>
  </div>
  <div style="float:right;">
    <div class="c_element color2">
          <p><br/>
        <b>Tags:</b><br/>
        <%= @post.tag_list %>
      </p>
    </div> 
    <% @post.elements.each_index do |i| %>
      <% if i >= 2 and i.even? and @post.elements[i].id != @element.id %>
        <div class="c_element color2">
          <%= render_element(@post,@post.elements[i]) %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
<div class="c">
  <div class="c_element color2">
    <p><%= link_to 'Zurück', @post %></p>
  </div>
  <div class="c_element color2">
    <%= add_tweet %>
    <%= add_buzz %> 
    <%= add_fcbk(request.url,'light') %>
  </div>
  <% @post.elements.each_index do |i| %>
    <% if i < 2 and @post.elements[i].id != @element.id %>
      <div class="c_element color2">
        <%= render_element(@post,@post.elements[i]) %>
      </div>
    <% end %>
  <% end %>
  <% @post.elements.each_index do |i| %>
    <% if i >= 2 and i.odd? and @post.elements[i].id != @element.id %>
      <div class="c_element color2">
        <%= render_element(@post,@post.elements[i]) %>
      </div>
    <% end %>
  <% end %>
</div>
